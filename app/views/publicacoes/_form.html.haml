= semantic_form_for @publicacao do |f|
  - if @publicacao.errors.any?
    #error_explanation
      %h2= "#{pluralize(@publicacao.errors.count, "error")} prohibited this publicacao from being saved:"
      %ul
        - @publicacao.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.input :titulo, label: 'Título'
  .field
    = f.input :abstract, label: 'Abstract'
  .field
    = f.input :link, label: 'Link'
  .actions
    = f.submit 'Save'
